# ================================================================
# Docker Build Context Exclusions
# ================================================================
# Only exclude files that are NOT needed for building the app

# Git files
.git
.gitignore
.gitattributes

# IDE files
.vs
.vscode
*.user
*.suo
*.sln.docstates

# Node (if used)
node_modules
npm-debug.log

# Documentation (not needed for runtime)
*.md
LICENSE

# Build outputs (will be created fresh in the container)
# NOTE: Do NOT exclude Components, Models, Services, or DTOs
# NOTE: Do NOT exclude .razor files or .cs files
bin/
obj/

# Docker files (don't copy into image)
Dockerfile*
docker-compose*
.dockerignore

# Environment files (secrets)
.env
.env.*

# IDE configurations
.editorconfig
azds.yaml

# Misc
*.log
.DS_Store
Thumbs.db
